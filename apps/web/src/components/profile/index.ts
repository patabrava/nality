/**
 * Profile Components
 * 
 * Components for displaying and editing user profile attributes
 * (values, influences, motto) from the user_profile table.
 */

export { ProfileCard } from './ProfileCard';
export { default as ProfileCardDefault } from './ProfileCard';
